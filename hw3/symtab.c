#include "symtab.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct symbol_table_list symtab;

// struct symbol_table_entry* find_symbol(char *name) {
//   for(int i = 0; i < symtab.size; i++) {
//     if(strcmp(name, symtab.symbol_table[i].name))
//   }
// }

int semantic_check(struct Node *node) { return 0; }
